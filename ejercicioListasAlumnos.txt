package com.company;
import java.util.*;

public class Main {

    private static  List<Alumno> listaAlu= new ArrayList<Alumno>();

    public static void main(String[] args) {

        cargaDatos();
        mostrarDatos();
     // mostrarAlumno(2);
     //  eliminarLista();
       // eliminarElemento();
        verificarDatos();
      //  actualizarDatos();

    }

    public static void actualizarDatos(){
       /* listaAlu.set();
        Iterator it=listaAlu.iterator();
        while(it.hasNext())
        {
            System.out.println(it.next());
        }*/
    }

    public static void eliminarElemento(){
        listaAlu.remove(1);
    }
    public static void eliminarLista(){
        listaAlu.clear();
    }

    public static void verificarDatos(){
        if(listaAlu.isEmpty()){
                 System.out.println("No contiene datos la lista");
        }
        else {
                System.out.println("Tiene datos la lista");
        }
    }

    private static void mostrarAlumno(int Index){

        Alumno A1= new Alumno();
        A1= listaAlu.get(Index);
        System.out.println(A1.getNombre());

    }

    private static void mostrarDatos(){

        System.out.println("SE MOSTRARAN "+listaAlu.size()+" ALUMNOS");

        for(Alumno alu: listaAlu){
            System.out.println("------------------------------------------");
            System.out.println("Legajo: "+alu.getLegajo());
            System.out.println("Nombre: "+alu.getNombre());
            System.out.println("Apellido: "+alu.getApellido());
            System.out.println("Dni: "+alu.getDni());
            System.out.println("Mail: "+alu.getMail());
            System.out.println("------------------------------------------");

        }

    }
    private static void cargaDatos(){
        Scanner entrada= new Scanner(System.in);
        String opc;


        do {
            Alumno a = new Alumno();

            System.out.println("ingrese legajo: ");
            a.setLegajo(entrada.nextInt());

            System.out.println("ingresar nombre: ");
            a.setNombre(entrada.next());

            System.out.println("ingresar apellido: ");
            a.setApellido(entrada.next());

            System.out.println("ingresar dni: ");
            a.setDni(entrada.nextInt());

            System.out.println("ingresar mail: ");
            a.setMail(entrada.next());

            listaAlu.add(a);

            System.out.println("desea seguir cargando: ");
            opc = entrada.next();

        } while (opc.equalsIgnoreCase("si"));
    }


}


package com.company;

public class Alumno {
    int legajo;
    String nombre;
    String apellido;
    int dni;
    String mail;


    public int getLegajo() {
        return legajo;
    }

    public void setLegajo(int legajo) {
        this.legajo = legajo;
    }

    public String getNombre(){
        return nombre;
    }

    public void setNombre(String nombre){
         this.nombre= nombre;
    }

    public String getApellido(){
           return apellido;
    }

    public void setApellido(String apellido){
        this.apellido=apellido;
    }

    public int getDni() {
        return dni;
    }

    public void setDni(int dni) {
        this.dni = dni;
    }

    public String getMail() {
        return mail;
    }

    public void setMail(String mail) {
        this.mail = mail;
    }
}
